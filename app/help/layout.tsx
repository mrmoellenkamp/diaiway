import type { Metadata } from "next"

export const metadata: Metadata = { title: "Hilfe & Support – diAiway" }

export default function HelpLayout({ children }: { children: React.ReactNode }) {
  return children
}
